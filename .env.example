# Jev (TypeSafe System One) through OpenRouter's Decisions endpoint.
# Only needed to RECORD cassettes. Replaying needs no key and no network.
OPENROUTER_API_KEY=

# Optional overrides.
JEV_BASE_URL=https://openrouter.ai/api/alpha/decisions
JEV_MODEL=typesafe/jev-1.13
JEV_ZDR=true
